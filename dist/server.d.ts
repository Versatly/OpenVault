/**
 * OpenVault MCP Server
 *
 * Exposes vault_search, vault_write, vault_forget, vault_status, vault_context
 * over stdio transport (default) — compatible with Cursor, Claude Code, Windsurf.
 */
export {};
